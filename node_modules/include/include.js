exports.include = function (file_) {
  var fs = require('fs');
  if (!fs.existsSync) {
    throw new Error('Unable to include `file_`');
  }
  with (global) {
    eval(fs.readFileSync(file_) + '');
  };
};